'use client';

import Header from '@/components/layout/Header';

/**
 * Common Navigation component for all pages
 * Wraps the Header component
 */
export default function Navigation() {
    return <Header />;
}
